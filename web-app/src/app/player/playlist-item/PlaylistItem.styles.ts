import styled from 'styled-components';
import { CenteredRow } from '../../../common';

export const PlaylistItemContainer = styled(CenteredRow)`
    justify-content: space-between;
    width: 90%;
    height: 100px;
    padding: 20px;
`;