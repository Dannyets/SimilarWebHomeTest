import styled from 'styled-components';
import { Row, Button, Input } from '../../../common';

export const AddSoungInputContainer = styled(Row)`
    margin: 10px;
`;

export const AddInput = styled(Input)`
    width: 100%;
`;

export const AddButton = styled(Button)`
`;